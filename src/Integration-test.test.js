import React from "react";
import { render, screen } from "@testing-library/react";
import App from "./App";

test("major parts of the system successfully work together", async () => {
  render(<App />);

});